[project]
name = "mcp-servers"
version = "0.1.0"
description = "Standalone MCP tool servers for the learning coach system"
requires-python = ">=3.11"
dependencies = [
    "mcp==1.26.0",
    "logger-lib",
]

[tool.uv.sources]
logger-lib = { path = "../logger_lib", editable = true }

[tool.uv]
package = false
