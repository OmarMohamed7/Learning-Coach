[project]
name = "logger-lib"
version = "0.1.0"
description = "Shared logging setup for the learning coach app and its MCP servers"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["logger"]
